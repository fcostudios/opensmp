# Ledger — Environment Variables (serverless / Next.js)
#
# The runtime env lives in apps/web/.env — copy apps/web/.env.example to
# apps/web/.env and fill it in. This root file is a pointer + the shared
# database contract.

# DATABASE_URL — the db client + verify-schema.mjs auto-select the driver
# by this URL (IMP-259): a standard Postgres URL → node-postgres (local dev
# + CI; `drizzle-kit push` applies locally); a *.neon.tech URL → neon-http
# (prod). Override with DB_DRIVER=pg|neon.
#   Local dev (default):
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/app
#   Prod (Neon): DATABASE_URL=postgresql://USER:PASSWORD@HOST.neon.tech/DB?sslmode=require
