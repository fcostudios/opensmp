// organism.queue-card — DoR stub (IMP-241). Props/body authored by the dev team
// from the Step-7 mocks; final UI is mock-driven.
export interface QueueCardProps {
  // intentionally minimal — richer extract absent (count-only)
}

export function QueueCard(_props: QueueCardProps) {
  return <section data-organism="queue-card">{/* TODO: implement from SCR mocks */}</section>;
}
