// organism.app-shell — DoR stub (IMP-241). Props/body authored by the dev team
// from the Step-7 mocks; final UI is mock-driven.
export interface AppShellProps {
  // intentionally minimal — richer extract absent (count-only)
}

export function AppShell(_props: AppShellProps) {
  return <section data-organism="app-shell">{/* TODO: implement from SCR mocks */}</section>;
}
