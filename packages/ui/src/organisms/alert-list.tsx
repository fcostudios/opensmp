// organism.alert-list — DoR stub (IMP-241). Props/body authored by the dev team
// from the Step-7 mocks; final UI is mock-driven.
export interface AlertListProps {
  // intentionally minimal — richer extract absent (count-only)
}

export function AlertList(_props: AlertListProps) {
  return <section data-organism="alert-list">{/* TODO: implement from SCR mocks */}</section>;
}
