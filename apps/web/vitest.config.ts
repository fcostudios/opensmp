// Vitest config (generated by Nous, IMP-305 / TE-2). A package can have the
// runner wired before it has any tests; `passWithNoTests` keeps that GREEN.
// The dev team adds tests per docs/dev-guide/TESTING.md §4 — an empty suite
// is not a failure, a broken one is.
import { defineConfig } from "vitest/config";
import { fileURLToPath } from "node:url";

const focusedPactRun = process.argv.some((argument) =>
  argument.replaceAll("\\", "/").endsWith(".pact.test.ts"),
);

export default defineConfig({
  resolve: {
    alias: {
      "@": fileURLToPath(new URL("./src", import.meta.url)),
    },
  },
  test: {
    environment: "node",
    passWithNoTests: true,
    include: ["src/**/*.{test,spec}.{ts,tsx}"],
    exclude: focusedPactRun ? [] : ["**/*.pact.test.ts"],
    hookTimeout: 120_000,
    testTimeout: 120_000,
  },
});
