# Generated by Nous (IMP-248 / I03 — DO NOT hand-edit).
# pnpm 11 blocks dependency build scripts by default; this ALLOW list
# (pnpm 11's `allowBuilds`, the successor to v10 `onlyBuiltDependencies`)
# lets the native toolchain deps build so `pnpm <script>` runs day one.
allowBuilds:
  esbuild: true
  sharp: true
  unrs-resolver: true
