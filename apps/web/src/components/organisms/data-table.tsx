// organism.data-table — DoR stub (IMP-241). Props/body authored by the dev team
// from the Step-7 mocks; final UI is mock-driven.
export interface DataTableProps {
  // intentionally minimal — richer extract absent (count-only)
}

export function DataTable(_props: DataTableProps) {
  return <section data-organism="data-table">{/* TODO: implement from SCR mocks */}</section>;
}
