// organism.pool-gauge — DoR stub (IMP-241). Props/body authored by the dev team
// from the Step-7 mocks; final UI is mock-driven.
export interface PoolGaugeProps {
  // intentionally minimal — richer extract absent (count-only)
}

export function PoolGauge(_props: PoolGaugeProps) {
  return <section data-organism="pool-gauge">{/* TODO: implement from SCR mocks */}</section>;
}
