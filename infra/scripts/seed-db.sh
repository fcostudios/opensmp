#!/usr/bin/env bash
set -euo pipefail

echo "=== Seeding development database ==="
echo "No seed script is generated for this project yet."
echo "Add one (e.g. a Drizzle insert script run with 'pnpm tsx') and"
echo "wire it here. Requires DATABASE_URL pointing at your managed Postgres."
echo "(No-op: nothing was seeded. This exits 0 by design so setup.sh continues.)"
